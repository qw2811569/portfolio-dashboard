# Claude

**Decision:** yes, adopt staged T0/T1 review.

Key points:

- T0 can run immediately, but it must be visibly labeled preliminary.
- T1 should gate on data presence, not on a fixed delay.
- Keep the T0 record instead of silently overwriting it.
- If T1 materially changes the thesis, users should be told why.

Raw summary from CLI:

> Yes, adopt T0/T1 staged review.
> T1 gates on data, not time.
> T0 must be visibly labeled preliminary.
> Retain the T0 record.
