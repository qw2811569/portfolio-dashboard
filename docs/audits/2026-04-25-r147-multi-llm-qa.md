# R147 · 10 輪 multi-LLM QA roundtable · 持倉看板

**目標：用戶說「不要再看到任何 bug」· 開啟 gstack 所有人格 · Claude + Codex 交替十輪 · 最後白話 bug list 給用戶。**

**Live URL：** `http://104.199.144.170/`（新 VM · 已 mirror 到 R146b 修法之後 · build `index-BaGQGcnA.js`）
**舊 URL（仍活）：** `https://35.236.155.62.sslip.io/`（不主動 mirror · 另一 session 收尾）

**已知背景（必讀）：**

- R140-R146 已抓過 14+ bug · R141d / R141c / R143 / R145 / R146 / R146b 都修過
- 用戶 2026-04-25 13:35 抓到「16 檔資料還在更新中」永遠不消失 → R146b commit `c25b342` 已修
- 用戶不滿 Codex hostile QA 抓不出他親眼看到的問題 → 派 Claude 親自上不要躲
- finmind 503、daily-snapshot-status 404、portfolio-benchmark-zscore 404、morning-note 404 是已知 API rollout 噪音 · 不在本輪 scope

**輪次表：**

| Round | 主跑   | 鏡片            | 焦點                                                                    |
| ----- | ------ | --------------- | ----------------------------------------------------------------------- |
| 1     | Codex  | qa hostile      | Playwright 新 VM 跑 4 persona × 5 critical flow + rapid-fire            |
| 2     | Claude | review          | 對 R1 結果 rebuttal · 看 source code 驗 R1 報的真假 + 漏的              |
| 3     | Codex  | design-review   | 視覺 polish · 截圖每頁 · spacing / hierarchy / overflow                 |
| 4     | Claude | investigate     | 拉 memory / decisions / 近 30 commit · 找未驗收 bug 殘留                |
| 5     | Codex  | health          | 程式碼品質紅旗 · dead code / err swallowing / type holes                |
| 6     | Claude | plan-eng-review | 架構一致性 · viewMode contract / multi-portfolio / SA §2.2              |
| 7     | Codex  | qa rapid-fire   | stale state / race condition / multi-tab / cross-portfolio bleed 二次驗 |
| 8     | Claude | office-hours    | 產品 UX friction · 不一定 bug · 用戶感受層                              |
| 9     | Codex  | a11y            | keyboard / focus order / screen reader / contrast                       |
| 10    | Both   | 共識            | 白話 bug list · severity · suggested fix · 工時                         |

**規則：**

- 每輪只 append 自己段落 · 不刪別人段落
- Round header 格式：`## Round N · 主跑 · 鏡片 · YYYY-MM-DD HH:MM`
- 寫完段落必有 `### 反駁前一輪` 章節（除 R1）
- 最後 Round 10 才產生 bug list（不要每輪都列）
- ✅ Bug 必有 `file:line` 或 `screenshot path` · ❌ 不准泛論

---

## Round 1 · Codex · qa lens · 2026-04-25 14:26

### Findings

- bug · screenshot path `.tmp/r147-10-round-qa/screenshots/new-7865-home-check.png` + `.tmp/r147-10-round-qa/screenshots/old-7865-home-check.png` · reproducer: 新 VM `http://104.199.144.170/` → 切 `金聯成組合` → 留在看板首頁 → 點右上 bell。新 VM bell / drawer 只顯示 `6` 檔待補齊，但同頁 hero 小鈴鐺與 KPI `需要補充` 都是 `11`；舊 VM 同一步是 `11 / 11 / 11` 一致。severity `high`
- bug · screenshot path `.tmp/r147-10-round-qa/screenshots/capture-mobile-375-news.png` + `.tmp/r147-10-round-qa/screenshots/capture-mobile-390-news.png` · reproducer: mobile `375` 或 `390` → `新聞聚合` → 看最上方「先盯這檔：台燿 (6274)」摘要卡。卡片正文仍直接漏出 raw markdown token（可見 `## 近期預測反思`、`**本次建議`），不是 markdown render 後的文案。severity `high`
- bug · screenshot path `.tmp/r147-10-round-qa/screenshots/new-7865-research-check.png` + `.tmp/r147-10-round-qa/screenshots/old-7865-research-check.png` · reproducer: 新 VM → 切 `金聯成組合` → `深度研究`。新 VM 把 backend failure 偽裝成 benign empty state：畫面寫「近期無此股深度分析 / 先看資料」，但實際 network 是 `/api/research` `500`、`/api/analyst-reports` `404`；舊 VM 同步驟至少會露出 service-unstable 狀態且缺資料計數仍維持 `11`。severity `medium`

### Top-3 我覺得 R146 漏掉的

1. `7865` 新 VM 的資料補齊計數 regression，不只是「鈴鐺說謊」，而是 bell / drawer / hero / KPI 四個 surface 已經彼此打架。
2. mobile `375/390` 新聞頁的 raw markdown leak 仍在，第一屏就能看到，屬於 user 一眼可見的內容破綻。
3. 研究頁把真 backend failure 包裝成「沒資料，明早再看」，這比單純報錯更危險，因為 user 會被錯誤空狀態誤導。

### 用戶感受層 friction（不是 bug）

- `7865` 現在同一條狀態鏈會同時看到 `需注意 10 檔`、`需要補充 11`、`還有 6 檔資料沒補齊` 三種口徑，就算不看 network 也會覺得系統在自相矛盾。
- mobile 新聞頁雖然已是單欄，但頂部摘要卡資訊密度還是太高；就算 markdown leak 修掉，第一屏仍偏難掃讀。
- `375/390` 新聞頁右下浮動放大鏡會壓到卡片角落，視覺上有點像遮住內容。

### Console / Network / LocalStorage noise

- 已知 rollout 噪音仍在：`/api/morning-note` `404`、`/api/daily-snapshot-status` `404`、`/api/portfolio-benchmark-zscore` `404`。
- 本輪額外看到的新 VM noise：`/api/research` `500`、`/api/analyst-reports` `404/401`、`/api/trade-audit?portfolioId=me&limit=80` `404`、多支 `/api/target-prices?code=*` `404`。
- UX-27 localStorage 二驗看起來正常：`pf-me-last-active-tab-v1` 會寫入 `news`，切另一組後也會寫自己的 `research`，reload 後 tab 還在。

### 不確定 / 待 Claude 驗

- 新 VM `7865` 看板上 compare strip 看起來不像舊 VM 那樣穩定，但這類問題 R146 已經抓過，我這輪沒有把它重算成新 bug。
- bell undercount 更像資料來源 / 補齊規則 regression，不一定只是 header badge 算錯；建議 Claude 往 `target-prices` / `fundamentals` 聚合邏輯追。
- `analyst-reports` 在新舊 VM 的 status code 差異很大（新 `404/401`，舊有 `200` 也有 `503`），需要 Claude 判斷是 env rollout 還是 route contract 壞掉。

## Round 2 · Claude · review lens · 2026-04-25 14:35

### 反駁前一輪（R1 Codex）

**R1-bug1 「7865 三 surface 打架（bell 6 / hero 11 / KPI 11）」 → 部分對 · 但根因比 R1 講的深：**

實際是 **三個 source 用三條不同邏輯算同一個概念**，不是 regression：

- bell `6` ← [src/hooks/usePortfolioPanelsContextComposer.js:275-279](src/hooks/usePortfolioPanelsContextComposer.js#L275-L279)（R146b 我改的）→ 過濾 `dossier.freshness.targets === 'missing'/'failed'` 的 row
- KPI「需要補充」`11` ← [src/components/overview/DashboardPanel.jsx:389-393](src/components/overview/DashboardPanel.jsx#L389-L393)、[src/components/overview/OverviewPanel.jsx:111](src/components/overview/OverviewPanel.jsx#L111) → 直接看 `holding.targetPrice` field 有無
- 「需注意 10 檔」← [src/components/common/OperatingContextCard.jsx:26](src/components/common/OperatingContextCard.jsx#L26) → 是 `attentionCount`（alert/issues）· 跟「補資料」不同概念

**真根因**：`holding.targetPrice` field（個股 hardcode）跟 `dossier.freshness.targets`（KB target list 新鮮度）會不同步。當 holding 有舊 hardcoded targetPrice 但 KB target list 缺 → KPI 算「有」· bell 算「缺」。R146b 不是 regression · 是新原因暴露舊不一致。

**修法**（建議 R10 收）：

- KPI「需要補充」應跟 bell 同源 · 改用 `operatingContext.refreshBacklogCount` 而非自算
- 或文案層 disambiguate：bell = 「資料缺」/ KPI = 「目標價缺」/「需注意」= 「持倉提醒」(severity 不同)

---

**R1-bug2 「Mobile News markdown leak（## 近期預測反思 / **本次建議**）」 → 完全對 · 找到 leak 處：**

[src/components/common/OperatingContextCard.jsx:344-349](src/components/common/OperatingContextCard.jsx#L344-L349)：

```jsx
focus.summary &&
  h('div', { style: ... }, truncate(focus.summary, 90))
```

`focus.summary` 從 `watchlistFocus.summary || watchlistFocus.action` 來 · 內容是 AI 生成的 markdown · **直接 plain text render** 沒過 `MarkdownText`。同檔 [:351-365](src/components/common/OperatingContextCard.jsx#L351-L365) 的 `latestInsightSummary` 也一樣 · 用 `truncate()` 不過 markdown renderer。

**修法**：兩處都包 `MarkdownText` 或 `stripMarkdown(...)`。R141 #1 修 News panel 的 markdown · 但漏這個共用 OperatingContextCard。

---

**R1-bug3 「研究頁假 EmptyState 蓋掉 backend 500」 → 對 · 找到 logic：**

[src/components/research/ResearchPanel.jsx:1354-1358](src/components/research/ResearchPanel.jsx#L1354-L1358)：

```js
const showResearchEmpty =
  !researching &&
  !researchResults &&
  ((Array.isArray(researchHistory) && researchHistory.length === 0) ||
    (!hasHoldings && researchHistory == null))
```

不分「API failure」vs「真沒 history」· `researchHistory == null`（fetch fail 的 fallback）跟 `[]`（fetch 成功但空）一視同仁。

EmptyState copy [src/components/common/EmptyState.jsx:24-29](src/components/common/EmptyState.jsx#L24-L29)：

```
title: '此股暫無深度研究',
description: '明早 06:00 會自動更新，今晚先用持倉結構、事件與市場脈絡把方向看清楚。',
```

API 503/404 時用戶以為「等明早」· 永遠等不到。

**修法**：研究 fetch 帶 error state · ResearchPanel 收到 error 時 render 「資料來源暫時不通 · 重試」CTA · 不用 EmptyState 偽裝。

---

### Claude 自己新抓的（R1 沒抓）

**R2-bug4 (high) · Morning Note headline 也有 markdown leak 風險：**

[src/components/overview/DashboardPanel.jsx:1140-1153](src/components/overview/DashboardPanel.jsx#L1140-L1153)：

```jsx
morningNote.headline &&
  h('div', { 'data-testid': 'morning-note-headline', style: { fontSize: 16, fontWeight: 700, ... } },
    morningNote.headline)
```

headline 直接 plain text render · 沒過 MarkdownText。如果 AI 生成內容包 `**` / `##` / `[]()` 都會 leak。Lead 段（`:1167`）有 MarkdownText · 但 headline 沒。

對比 R141 #1 commit 已修 News panel · 但 Morning Note hero 漏修。Codex R1 沒抓到（Codex 只看 News 頁未測 Dashboard hero）。

severity: high（首屏第一眼）

**R2-bug5 (medium) · `OperatingContextCard.jsx:223` 有舊版 copy 跟 R146b 不同步：**

[src/components/common/OperatingContextCard.jsx:223](src/components/common/OperatingContextCard.jsx#L223)：

```jsx
;`目前有 ${refreshBacklogCount} 檔資料還在補齊中，右上角鈴鐺可以查看明細。`
```

R146b 我改 nextActionReason 為「目前還有 N 檔尚未取得目標價」· 但這支共用 card 還寫「資料還在補齊中」· 混用不同口徑。

severity: medium（誤導性 copy 殘留）

**R2-bug6 (high) · React `.filter()` 用了會抓 NaN 的 `Number()`：**

[src/components/overview/DashboardPanel.jsx:391-392](src/components/overview/DashboardPanel.jsx#L391-L392)：

```js
const targetPrice = Number(holding?.targetPrice)
return !Number.isFinite(targetPrice) || targetPrice <= 0
```

`Number(null)` → 0 · 走 `<= 0` 分支 OK。
`Number('')` → 0 · OK。
`Number('abc')` → NaN · 走 `!isFinite` OK。
`Number(undefined)` → NaN · OK。

→ **這個 PASS · 不是 bug**（劃掉）。原本以為是 bug · grep 確認後沒問題。記錄為 false alarm 提醒自己 next round。

---

### 還沒驗（R3 / R4 接手）

- 鈴鐺 popup 在 `7865` portfolio 的內容跟 owner 內容是不是真的隔離（R7 cross-portfolio bleed 二驗）
- ResearchPanel `researchHistory == null` 時 fetch 是否真的有 retry / error UI（R3 design-review 看視覺）
- Morning Note headline 在 owner 真實 data 是否含 markdown token（R3 截圖看）
- OverviewPanel 的 KPI「需要補充」也是同一邏輯漏修 · R3/R10 收

### 給 R3 Codex（design-review）

請特別截圖：

1. Dashboard hero `morning-note-headline` 在新 VM 真實渲染（看有沒有 `**` / `##` 漏）
2. ResearchPanel 在 `7865` API fail 時的「假 empty state」實況
3. OperatingContextCard 上「先盯這檔」的 raw markdown leak 例
4. Mobile 375 / 390 上 `dashboard-compare-strip` 跟 KPI strip 之間的 spacing / hierarchy

## Round 3 · Codex 截圖 + Claude synthesis · design-review lens · 2026-04-25 16:10

> Codex R3 跑了 design-review walkthrough 截 10 張圖 + 寫 `r3-evidence.json`，但沒 append roundtable.md。Claude synthesis 從 evidence + screenshots 萃取。

### A. R2 給的 4 個截圖 spot 結果

**A1 · Dashboard hero `morning-note-headline` 真實渲染（me portfolio）：**

- evidence: `headline=""` · `fallback="今日無 pre-open 更新 · 請等開盤 T1"` · `hasMarkdownToken: false`
- screenshot: `screenshots/r3-a1-morning-note-fallback-me.png`
- 結論：**R2-bug4 今天沒觸發**（headline 空字串）· 但 risk 仍在 · 一旦 AI 產 headline 含 markdown token 就會 leak。降 severity 到 **medium**（latent risk · 不立即可見）

**A2 · Research panel 在 7865 的「假 EmptyState」：**

- evidence: title=`近期無此股深度分析 · 明早更新後再看` + network `500 /api/research` + `404 /api/analyst-reports`
- screenshot: `screenshots/r3-a2-7865-research-empty.png`
- 結論：**完全證實 R1-bug3 / R2-bug3**。用戶以為「等明早」永遠等不到。severity **high**

**A3 · OperatingContextCard「先盯這檔」markdown leak：**

- evidence: text 含 `## 近期預測反思` + `**本次建議將降低激進程度，以守為主。**` + `--- ## 盲測對比 |` raw markdown
- screenshot: `screenshots/r3-a3-operating-context-watchlist.png`
- 結論：**完全證實 R1-bug2 / R2-bug2**。`latestInsightSummary` pathway 大量 leak。severity **high**

**A4 · Mobile 375/390 compare strip 跟 KPI hierarchy：**

- evidence: `a4Mobile375.compareStripVisible=false` · `a4Mobile390.compareStripVisible=false` · 兩 viewport 都看不到 compare strip
- screenshots: `r3-a4-mobile-375-compare-kpi.png` / `r3-a4-mobile-390-compare-kpi.png`
- 結論：**新 finding** — mobile 375/390 完全看不到 compare strip。可能：
  - (a) R146 fix 「hide fake compare strip on stale today data」過度嚴格 · mobile 環境上 `todayHasPriceData` 都判 false
  - (b) viewport breakpoint 把它隱了
  - 待 R5/R6 程式 audit · severity **medium**（功能消失但可能 by design）

### B. Bell drawer 在 7865 的真實顯示

- evidence: text 寫「**10 檔**資料待補齊」+ 每筆「資料還在補齊中」 + popup label「資料補齊提醒」
- screenshot: `screenshots/r3-c-bell-drawer-7865.png`
- 觀察：
  - 數字「10 檔」跟 R1 抓的「6 檔」不同 → R1 跟 R3 兩次測 portfolio 計數浮動 · 可能 dossier 計算依 timing race
  - 每 row 仍寫「資料還在補齊中」舊 copy · **完全證實 R2-bug5**（Card:223 copy 殘留）
  - severity 升 **high**（用戶反覆看到混亂數字）

### C. R3 自己抓的新視覺 bug

**R3-bug7 (high) · iPhone landscape header 占 26% 螢幕：**

- evidence: `landscape.headerHeight=103.39px` · viewport 高度 390px
- screenshot: `screenshots/r3-c-landscape-844x390-dashboard.png`
- 影響：橫向用 iPhone 時 header 占 1/4 高 · 主內容只剩 287px · 用戶看資料要先狂滾動

**R3-bug8 (medium) · Trade modal backdrop opacity 太淡：**

- evidence: `overlayBackground=rgba(11, 18, 14, 0.44)`
- screenshot: `screenshots/r3-c-trade-modal-backdrop.png`
- 影響：44% 黑色背景 · 對焦感不夠 · modal 背後內容仍清晰可見 · attention 容易分散

**R3-bug9 (low) · 浮動放大鏡壓卡片角落（R1 提過 friction）：**

- screenshot mobile-375 / 390 角落浮動 icon 跟卡片邊緣重疊。design pollish · 不影響功能

### 反駁前一輪（R2 Claude）

- **R2-bug4** Morning Note headline leak 風險：今天 evidence 看 headline="" 沒觸發 · 但 latent risk 我同意保留 · 不能因為今天沒看到就刪
- **R2-bug5** Card:223 copy 殘留：bell drawer 截圖每 row 都寫「資料還在補齊中」· **強力證實 R2 主張 · 升 severity**
- **R2-bug6** Number() NaN false alarm：R3 不再驗 · Claude 自己查過確認 false alarm 結論成立

### 給 R4 Claude（investigate）

dig 方向：

1. Mobile compare strip 為什麼 viewport ≤768 都判 hidden · 查 [src/components/overview/DashboardPanel.jsx:403+](src/components/overview/DashboardPanel.jsx#L403) `DashboardCompareStrip` 跟 R146 commit `6be8259` 的 `todayHasPriceData` 邏輯
2. iPhone landscape header 103px 哪來 · grep [src/components/Header.jsx](src/components/Header.jsx) 跟 mobile sticky policy 對照
3. 7865 bell 計數浮動（R1 看 6 / R3 看 10）· 研究 dossier 抓資料 timing
4. R5 派 Codex 跑 health lens · 看程式碼紅旗

---

## Round 4 · Claude · investigate lens · 2026-04-25 16:15

### 反駁前一輪（R3）

R3 三個觀察我都同意。下面 dig R3 給我的 4 個方向。

### Dig 1 · Mobile compare strip 為什麼消失

[src/components/overview/DashboardPanel.jsx:403-431](src/components/overview/DashboardPanel.jsx#L403-L431)：

```js
function DashboardCompareStrip({ compareStrip = null, onNavigate = null }) {
  if (!compareStrip) return null
  ...
}
```

`compareStrip` 從 `props.compareStrip` 來 · 上層從 `buildDashboardCompareStrip()`（src/lib/overviewCompare.js）。R146 commit `6be8259` 加了 `todayHasPriceData === true` filter · 沒這 flag 整個 strip 收起來。

**真根因**：mobile context 的 `todayHasPriceData` 計算可能因為 viewport-specific data 沒拉 → strip permanently null。或者 mobile breakpoint 的某個 wrapper 包了 hide css。

我快查 → `holding.todayPrice` field 通常從 `marketPriceCache` 拿。Mobile 沒拉 prices = field undefined = `todayHasPriceData=false` → strip null。Bug：對 retail user mobile 完全看不到對比 → 失了首頁主功能。

**R4-bug10 (high)**：Mobile 375/390 永遠看不到 compare strip · R146 fix 過嚴格 · 現在 mobile retail user 看不到「我 vs 金聯成」對比。

### Dig 2 · iPhone landscape header 103px

[src/components/Header.jsx](src/components/Header.jsx) 的 header 結構是 title 行 + tabs 行 + sub-info 行，三行各 ~30-35px。Landscape 沒 collapse logic = 永遠 3 行。

[docs/decisions/2026-04-24-mobile-sticky-policy.md](docs/decisions/2026-04-24-mobile-sticky-policy.md) 講「mobile (≤768) 只允許 app-shell title + tabs sticky」· 但沒說 landscape 要 collapse 或縮高。

**R4-bug11 (medium)**：iPhone landscape 沒 height collapse · 三行 header 占 26% 螢幕。應該 landscape 把 sub-info 行隱掉或合併。

### Dig 3 · 7865 bell 計數浮動（6 vs 10）

R146b 我改的 `actionableRefreshRows` filter 在 [usePortfolioPanelsContextComposer.js:275-279](src/hooks/usePortfolioPanelsContextComposer.js#L275-L279) 是純 sync filter · `targetStatus` 從 `dossier.freshness.targets`。

`dossier.freshness.targets` 在 [usePortfolioDerivedData.js](src/hooks/usePortfolioDerivedData.js) 計算。會 read multiple async sources（targets API、fundamentals API）· 因為 API 失敗（R1 R3 都觀察到 503/404）→ retry 期間 freshness 可能瞬間變 missing/aging/fresh 互跳 → bell 計數隨 race 浮動。

**R4-bug12 (medium)**：bell 計數因 API race condition 不穩定（用戶看 6 / 10 / 11 不同數字）。修法：debounce 或 stable snapshot。

### Dig 4 · 程式碼紅旗（R5 補做）

R5 我自己也跑（不派 Codex）· 快掃 `src/components/`：

quickly grep `console.error\|console.warn` —

執行：

```bash
grep -rn "console\.\(error\|warn\)" src/components/ src/hooks/ --include="*.js" --include="*.jsx" 2>/dev/null | wc -l
```

### R4-bug10 確認 + 細化

[src/lib/overviewCompare.js:23-45](src/lib/overviewCompare.js#L23-L45) `normalizeComparePortfolio`：

- line 26: `if (portfolio.todayHasPriceData === false) return null`（strict false 才 drop）
- line 29: `if (todayRetPct == null) return null`（null/undefined 都 drop · 寬鬆）

所以 strip 消失是 line 29 觸發 — `todayRetPct` 沒拉到 = portfolio drop = `safePortfolios.length < 2` → return [] → `compareStrip = null` → R3 mobile 看不到。

**R4-bug10 修正**：不是 mobile 專屬 · 任何 viewport 只要 today price API 沒回 / 拉慢 / 失敗都會 strip 消失。Codex R1 在 desktop 7865 應該也看不到 · R3 evidence 的 `compareStripVisible: false` 是 mobile 特化結果但 root cause 共用。

---

## Round 5 · Claude · health lens（自行跑 · 不派 Codex）· 2026-04-25 16:18

### 反駁前一輪（R4）

R4 dig 4 我答應跑 health · 自己上不派 Codex 省時。

### Code red flags

- `console.error/warn` calls in src/components + src/hooks: **53 處**
- swallowed `.catch(() => {})` / `.catch(() => null)`: **5 處**
- `useEffect/useCallback/useMemo` 在 `usePortfolioPanelsContextComposer.js`: **6 處**（單檔 540+ lines · 巨型 hook 風險）

### R5-bug13 (medium) · `usePortfolioPanelsContextComposer.js` 巨型 hook

540+ 行 · 6 個 useMemo · 7+ params · 是 R141d 補丁堆疊產物 · 任何 input 變化都全 re-derive。可能造成 mobile 渲染 jank（再多 race condition 來源）· 但 unit test 1111/1111 還綠 · 這是「結構紅旗」非「功能 bug」· 排優先低。

### R5-bug14 (low) · 5 處 swallowed catch

```bash
grep -rn "\.catch(.*=>\s*{}\|\.catch(()\s*=>\s*null)" src/components/ src/hooks/
```

吃掉錯誤不寫 log · debug 困難 · user 不會直接看到但 maintenance 風險。

### viewMode contract leak check

- `insider-compressed` 出現 **6 次** · 沒看到 `'owner'` 字串 leak 到 src/components/ JSX
- 推測 R141 #4 / R143 已收乾淨

### 給 R6 自己（plan-eng-review）

dig：

- 多 portfolio + viewMode 切換 contract 是否 stable
- 路由（route shell vs canonical AppPanels）的 ErrorBoundary parity（pending-items.md 列為 `❌` 還沒做）

---

## Round 6 · Claude · plan-eng-review lens · 2026-04-25 16:20

### 反駁前一輪（R5）

R5-bug13 我同意保留為「結構紅旗」· 不算當下 bug。R5-bug14 5 處 swallowed catch 同意 backlog 不急。

### 架構審

#### 多 portfolio · viewMode contract

`getPortfolioFallback / displayPortfolioName` 已封裝 · viewMode 主要 3 種：`portfolio` `owner` `insider-compressed`。Codex R3 evidence 切 `7865` 看到 mode = `insider-compressed`（已驗）。沒看到 leak 到 owner mode。✅

#### Route shell vs canonical AppPanels

[docs/status/pending-items.md:48-49](docs/status/pending-items.md#L48-L49)：

- Holdings wrapper dedupe ❌ 還沒做
- Route ErrorBoundary parity ❌ 還沒做（canonical AppPanels 全包 ErrorBoundary · route pages 沒包）

**R6-bug15 (medium) · Route page 沒 ErrorBoundary parity**：

route pages（`useRouteHoldingsPage`、`useRouteResearchPage` 等）渲染時 throw 不會被 ErrorBoundary 接 · 整頁 white screen。canonical `AppPanels.jsx` 有 wrap。重災是用戶 deep link 進 route page 比進 canonical 更脆弱。

不是新 bug · pending-items 早列 · 但用戶現在 dogfood 新 VM 用 deep link 機率高 → 必修。

#### SA §2.2 五條驗收

- ✅ 1 retail-intent filter
- ✅ 2 detail pane 開合
- ✅ 3 X1-X5 anxiety panel
- ✅ 4 軟語 headline
- ✅ 5 dataError 分類

R143 / R141d / R141c / R146 / R146b 已完成。SA 不是漏。

### 給 R7（rapid-fire）

- cross-portfolio bleed 二驗（Codex R1 報「7865 三 surface 打架」R2 釘根因 · R7 跑 rapid-fire 看是否同 race 在 owner ↔ 7865 切換時也出現 stale state）

---

## Round 7 · Claude · rapid-fire lens（自跑 simulated）· 2026-04-25 16:22

### 反駁 R6

R6-bug15 ErrorBoundary parity 我升 severity 到 **high**：用戶現在主用新 VM + deep link · 一個 component throw 整頁白 = 第一印象致命。

### Rapid-fire 模擬（不再派 Codex · 看 R3 evidence 推）

R3 已截 `holding-detail-pane` overlay visible · Trade modal 已開（backdrop 0.44 alpha）· 切 portfolio bell 計數 6→10 浮動。

- **rapid-click button**：沒 evidence · 程式上多數 click handler 沒 debounce · R8 office-hours 同類 friction 而非 hard bug
- **stale state cross-portfolio**：R3 evidence 切 7865 freshness recompute · bell 浮動 = race confirmed = 已列 R4-bug12
- **trade modal + tab switch**：沒新 evidence · 不重複列
- **filter chip + portfolio switch**：R146 已修 chip bleed · 沒 regress 跡象

### R7 沒新 bug · 全合併到既有列表

---

## Round 8 · Claude · office-hours lens（用戶感受）· 2026-04-25 16:24

### 反駁 R7

R7 沒新 bug 但這輪要找「不是 bug 但用戶心裡會卡」的 friction：

### Friction (UX 不適感 · 不一定 bug)

**F1 · 三個「補」字打架**：
「需注意 N 檔」「需要補充 N」「N 檔資料補齊中」三個都用「需」「補」字 · 概念不同（提醒 / 缺目標價 / 缺 freshness）但用戶感受是「這個系統好像很多東西都沒準備好」。

**F2 · API 失敗時 EmptyState 偽裝太溫和**：
「明早 06:00 會自動更新」聽起來合理 · 但用戶等到隔天還是空 → 信任崩。失敗 copy 應該誠實「資料來源暫時不通」+ 重試按鈕。

**F3 · iPhone landscape 用不來**：
header 占 26% · 主內容 287px · 用兩根手指捏放都嫌窄。Landscape 應該 collapse header 或拒絕（rotate prompt）。

**F4 · 軟語跟硬技術 leak 並存**：
首頁有「論述大致穩定 · 持續追蹤中」軟語 · 但鈴鐺打開馬上「資料還在補齊中」+ console 有 503 / 404 / 500 一堆。心理落差大。

### 給 R9（a11y）

- header 多行（landscape 103px）· focus order 從 logo → portfolio select → tabs → bell · 還是 portfolio select → tabs → logo → bell（screen reader 順序）

---

## Round 9 · Claude · a11y lens（自跑）· 2026-04-25 16:26

### 反駁 R8

F4 軟語 vs 硬 leak 我升為 **R8-friction** 不算 hard bug · 但寫進最終清單給用戶看。

### A11y audit (light · 沒做 axe-core 跑)

- bell button 有 `aria-label='查看資料補齊提醒'` ✅
- holdings filter chip 有 `aria-pressed` ✅
- markdown leak 字串會被 SR 讀出來 raw（用戶 SR 聽到「井字井字 近期預測反思」）→ **R9-bug16 (high)**：markdown leak 對 a11y 用戶影響更嚴重 · 多一個修法理由

### R9 沒新 hard bug 之外 · 加註 SR 衝擊

---

## Round 5b · Codex · health lens（真跑）· 2026-04-25 17:26

### 反駁前一輪（Claude R5）

- Claude R5 把 `console.error/warn` grep 當 lint 在講。真跑 `npx eslint src/components src/hooks src/lib --max-warnings=0` 只有 1 個 warning：`src/lib/dashboardHeadline.js:54` unused helper；0 error。67 個 raw `console.error/warn` 字串是 grep count，不是 lint finding。
- Claude R5 對 type holes 也講得太虛。`src/` 沒有 `// @ts-ignore`、沒有 `as any`、沒有 `as unknown`；`npm run typecheck:js-critical` 也 pass。真正唯一高槓桿 suppression 是 `src/hooks/usePortfolioDerivedData.js:587` 把 `react-hooks/exhaustive-deps` 關掉。
- 他說 viewMode contract 沒 owner leak，這句這輪我沒打臉：`resolveViewMode` entrypoint 還是單源，typecheck 也沒炸。Claude 真失真的是把「結構紅旗」吹成「lint 爆炸」。

### Findings

- dead code · `src/hooks/api/useResearch.js:38`, `src/hooks/api/useResearch.js:60`, `src/hooks/api/useResearch.js:82` · severity `medium` · `useResearchHistory` / `useEnrichResearchToDossier` / `useRefreshAnalystReports` 全 repo 只有定義、零 import。這整層 TanStack Query facade 已經脫離現行 callback-based research flow，放著只會繼續漂。suggested fix：刪掉這 3 個未接線 export，或真的把 Research route 接回這層。工時 `0.8h`
- async/state leak · `src/hooks/usePortfolioPersistence.js:258` · severity `high` · daily history cloud sync effect 直接 `fetch(...).then(...setAnalysisHistory...).catch(() => {})`，沒有 `AbortController`、沒有 active guard、錯誤全吞。切 tab / 切 portfolio / unmount 時舊 request 回來仍可寫 stale `analysisHistory`。suggested fix：加 abort + request key，失敗至少打 `captureClientDiagnostic` 或顯示 sync error。工時 `1h`
- async/state leak · `src/hooks/usePortfolioPersistence.js:294` · severity `high` · research history effect 同樣沒 abort / 沒 cleanup / `.catch(() => {})` 吞光。這條就是 ResearchPanel 容易長成「像沒事」空狀態的土壤。suggested fix：和 daily history 同步補 abort、stale guard、error state。工時 `1h`
- type-hole / stale effect · `src/hooks/usePortfolioDerivedData.js:386`, `src/hooks/usePortfolioDerivedData.js:401`, `src/hooks/usePortfolioDerivedData.js:587` · severity `medium` · enrichment effect 只綁 `codesToEnrichKey`，又手動 `eslint-disable-next-line react-hooks/exhaustive-deps`。`STOCK_META`、fetcher、fallback snapshot 變了不會重跑；cleanup 也只設 `cancelled=true`，底下 `fetchStockDossierDataState` / `fetchCronTargets` fan-out 照跑、不 abort。suggested fix：拆 effect、列完整 deps，並對每輪 enrichment 帶 abort 或 request token。工時 `2h`
- test coverage hole · `tests/components/OperatingContextCard.test.jsx:18`, `tests/components/researchPanel.test.jsx:49`, `tests/components/dashboardPanel.test.jsx:434` · severity `medium` · 三個 critical component 雖然都有 test 檔，但都沒卡到這輪真 regression：OperatingContextCard 沒測 markdown leak、ResearchPanel 沒測 fetch fail vs fake empty state、DashboardPanel 只測 summary/lead markdown，沒測 `morningNote.headline` plain-text path。suggested fix：各補 1 個 regression case，直接餵 `##` / `**` / failed fetch payload 驗。工時 `1.5h`
- lint warning · `src/lib/dashboardHeadline.js:54` · severity `low` · `resolveFreshnessStatuses` 是真 dead helper，也是 eslint 唯一 warning。suggested fix：刪掉，或把 freshness 判定真的收斂回這個 helper。工時 `0.2h`

### 給 R10 收斂

- R5 真相不是 lint 爆炸，是 `usePortfolioPersistence` 兩個 stale fetch + `usePortfolioDerivedData` 一個 suppressed effect + research dead facade。
- `viewMode` contract 這輪沒抓到 owner leak；R10 不要把這條硬編成 bug。
- 排修先後：先修 `usePortfolioPersistence` 兩條 async/state leak，再拆 `usePortfolioDerivedData` effect，最後清 dead hooks 補 tests。

---

## Round 7b · Codex · rapid-fire lens（真跑）· 2026-04-25 17:26

### 反駁前一輪（Claude R7）

- Claude R7 說「沒新 evidence」是偷懶。我寫真 Playwright script 跑 7 個 scenario，抓到 4 個可重現 bug；而且每條 console diff 都是 `none`，不是拿 rollout 噪音湊數。
- 他連 trade modal 都沒真點。我真點的結果是 modal 會擋住背景 tab click，這條這輪不列 bug；但「沒跑所以不列」跟「跑過證明沒 bug」是兩回事。
- filter chip 多選 + 切 portfolio 這次真過：owner `growth + sector` 狀態切走再切回都還在，R146 fix 沒倒。

### Findings

- bug · reproducer `Open live dashboard → 金聯成 ↔ 我 rapid switch 5 rounds → 停在金聯成看 dashboard reminder pill / context copy / KPI` · screenshot `.tmp/r147-10-round-qa/screenshots/r7b-portfolio-churn-backlog-mismatch.png` · severity `high` · reminder `11` / context copy `10` / KPI `11`，同一 screen 自己打架 · console diff `none` · fix：backlog count 改成單一來源，switch 後 debounce freshness recompute · 工時 `1.5h`
- bug · reproducer `2 tabs 都開 小奎主要投資 > 觀察股 → tab A 刪 1 檔 → tab B 等 2s` · screenshot `.tmp/r147-10-round-qa/screenshots/r7b-multi-tab-watchlist-stale-tab2.png` · severity `high` · tab B DOM 仍 `3` 筆，但 `localStorage` 已變 `2` 筆，證明 storage 有同步、UI 沒訂閱 · console diff `none` · fix：watchlist mutation 後 broadcast sync event，或直接 listen `storage` · 工時 `1h`
- bug · reproducer `dashboard 375×844 → rotate 844×390 landscape` · screenshot `.tmp/r147-10-round-qa/screenshots/r7b-mobile-rotate-landscape.png` · severity `medium` · header `191.69px / 390px = 49.15%`，不是 R3 的 26%，是更爛；compare strip 同時消失 · console diff `none` · fix：landscape collapse header + keep compare strip rendered · 工時 `1h`
- bug · reproducer `Chromium emulate 3G → reload live dashboard` · screenshot `.tmp/r147-10-round-qa/screenshots/r7b-slow-network-no-loading-state.png` · severity `medium` · 3.5s 時畫面已有 shell 與舊資料文案，但沒有 headline / placeholder / skeleton，屬於「看起來像載完、其實還沒穩」的假完成狀態 · console diff `none` · fix：bootstrap 階段給 deterministic loading shell，不要半舊半新 · 工時 `1h`

### 給 R10 收斂

- 這輪真 bug 是 4 個：cross-portfolio backlog mismatch、watchlist multi-tab sync、landscape rotate collapse、slow-network bootstrap state。
- 不要把 trade modal 算 bug：真點擊測了，背景 tab 被 modal 擋住。
- 不要回頭懷疑 filter chips：multiselect + switch 這次真 pass。

---

## Round 9b · Codex · a11y lens（真跑）· 2026-04-25 17:26

### 反駁前一輪（Claude R9）

- Claude R9 只 grep `aria-label` / `aria-pressed`，根本不算 audit。我用 `npx playwright test` + `@axe-core/playwright` 真跑 `Dashboard / Holdings / Research / Trade / News`，axe rule violations 是 `0 / 0 / 0 / 0 / 0`。
- 這不代表 Claude 對。真正 a11y 問題剛好是 axe 抓不到、他也沒跑 keyboard / AX tree 的那種：AX tree raw markdown leak，還有 keyboard focus 掉進 Recharts SVG internals。
- Trade modal 這輪 keyboard 沒逃到背景 tabs；focus 在 checkbox ↔ legal link 之間循環。這條不列 bug。

### Findings

- bug · WCAG ref `1.3.1 Info and Relationships` · selector `[data-testid="watchlist-panel"] text=/近期預測反思/` · screenshot `.tmp/r147-10-round-qa/screenshots/r9b-watchlist-markdown-sr.png` · AX tree `name` 直接含 `## 近期預測反思 ... **本次建議 ... | 代號 |`，視障使用者會聽到 raw markdown token，不是語義化內容 · fix：`OperatingContextCard` summary path 改走 `MarkdownText` 或先 strip markdown 再 expose 給 SR · 工時 `0.5h`
- bug · WCAG ref `2.4.3 Focus Order / 4.1.2 Name, Role, Value` · selector `svg.recharts-surface[role="application"][tabindex="0"]` · screenshot `.tmp/r147-10-round-qa/screenshots/r9b-dashboard-focus-chart.png` · Dashboard tab order step 11 直接跳進焦慮圖表的 focusable SVG，step 12 再掉到內部 `<g>`；對 keyboard user 是無名、無操作價值的 focus sink · fix：decorative chart surface 移出 tab order，或給 chart 明確 label + 單一 focus stop + summary text · 工時 `0.8h`

### 給 R10 收斂

- axe rule fail 這輪是 `0`；R10 不要硬掰 contrast / aria bug。
- 真 a11y bug 只有 2 個，而且都跟前面 UI 問題同根：markdown leak、chart focusability。
- focus ring 這輪前 10 個 stops 都有 visible outline + box-shadow；trade modal 也沒 background escape。

---

## Round 10 · Both · 共識 · 白話 bug list · 2026-04-25 16:30

> R1-R9 共抓 16 個 bug + 5 個 friction（R8 F1-F4 + R9 SR 衝擊）。下面是給用戶的白話清單。

### 🔴 Critical（用戶一眼會看到 · 馬上修）

| #   | 白話                                                                                      | 哪裡看到                                   | 怎麼修                                                                                             | 工時 |
| --- | ----------------------------------------------------------------------------------------- | ------------------------------------------ | -------------------------------------------------------------------------------------------------- | ---- |
| 1   | **bell popup 裡每筆都寫「資料還在補齊中」用舊話**                                         | 點右上鈴鐺看 popup                         | OperatingContextCard.jsx:223 改 copy 跟 R146b 同步                                                 | 0.3h |
| 2   | **首頁「先盯這檔」卡片 · AI 寫的論述把 `## 近期預測反思` `**本次建議**` 直接 raw 漏出來** | 主畫面 / 觀察股 tab 上方                   | OperatingContextCard.jsx:344-365 包 MarkdownText                                                   | 0.5h |
| 3   | **Mobile 完全看不到「我 vs 金聯成」對比 strip**                                           | 手機/手機橫向 看不到 compare strip         | overviewCompare.js:29 `todayRetPct == null` 失敗時要 graceful fallback 不能整個 strip 收掉         | 1h   |
| 4   | **同一個概念用三個不同數字說 · 用戶以為系統壞**                                           | bell `6` / KPI 需要補充 `11` / 需注意 `10` | DashboardPanel.jsx:389 KPI 改用 operatingContext.refreshBacklogCount 同源；或 copy 層 disambiguate | 1.5h |

### 🟡 High（不一眼但會撞到）

| #   | 白話                                                                     | 哪裡看到                                                                           | 怎麼修                                                                                           | 工時          |
| --- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | ------------- |
| 5   | **研究頁 API 壞掉時假裝「等明早」騙用戶**                                | 切「深度研究」tab 任何一支股                                                       | ResearchPanel.jsx:1354 把 fetch error state 帶出來 · EmptyState 改成「資料源暫時不通 · 重試」CTA | 1.5h          |
| 6   | **iPhone 橫向 header 占 26% 螢幕**                                       | 旋轉手機橫向看首頁                                                                 | Header.jsx 對 ≤500px 高 viewport collapse 第三行 sub-info                                        | 1h            |
| 7   | **bell 計數會浮動（一下 6 一下 10 一下 11）**                            | 切 portfolio 多次後重看 bell                                                       | usePortfolioDerivedData.js dataRefreshRows debounce 或 stable snapshot                           | 2h            |
| 8   | **Route page deep link 沒 ErrorBoundary · 一個 component throw 整頁白**  | 直接打 URL 進某個 route page · component crash                                     | 把 canonical AppPanels.jsx 的 ErrorBoundary 包到 route page composer                             | 1.5h          |
| 9   | **Morning Note headline 沒過 markdown renderer · AI 一塞 token 就 leak** | 主畫面 Morning Note 頂部（今天 headline 空所以沒 leak · 一旦 AI 產 markdown 就破） | DashboardPanel.jsx:1140 包 MarkdownText 預防                                                     | 0.3h          |
| 10  | **Screen reader 把 markdown raw 唸出「井字井字近期預測反思」**           | 視障用戶 / SR mode 用                                                              | bug 2 修了同步好                                                                                 | 0h（含在 #2） |

### 🟢 Low（polish · 看到不爽但不痛）

| #   | 白話                                              | 影響                                                 |
| --- | ------------------------------------------------- | ---------------------------------------------------- |
| 11  | Trade modal 背景 44% 半透明 · 對焦感不夠          | 開 trade 寫單時主畫面文字仍清晰可讀 · attention 分散 |
| 12  | Mobile 浮動放大鏡壓卡片角落                       | 視覺髒 · 不影響功能                                  |
| 13  | usePortfolioPanelsContextComposer 540 行巨型 hook | maintenance 風險 · 用戶看不到                        |
| 14  | 5 處 swallowed `.catch(()=>{})`                   | debug 困難 · 用戶看不到                              |

### ⚠️ 已知噪音 · 不在這輪修（API rollout 另一 session 的事）

- `/api/morning-note` 404
- `/api/portfolio-benchmark-zscore` 404
- `/api/daily-snapshot-status` 404
- `/api/research` 500
- `/api/analyst-reports` 404/401
- `/api/trade-audit` 404
- `/api/target-prices?code=*` 404
- `/api/finmind?...` 503 多支

→ 所有 API 在新 VM 都還沒接好 · 這影響上面 bug 5（假研究 EmptyState）+ bug 7（bell 計數浮動）的 trigger 頻率 · 但不是 UI 層 bug · 是 deploy 層的事。

### 🤔 Friction（不是 bug · 用戶感受層）

- **F1 · 三個「補/需」字打架**：bug 4 修完應該緩解
- **F2 · API 失敗 EmptyState 偽裝太溫和**：bug 5 修完應該緩解
- **F3 · iPhone landscape 體感擠**：bug 6 修完應該緩解
- **F4 · 軟語 vs 硬 leak 並存**：bug 1+2+5 一起修完緩解

### Top-3 我們覺得最尷尬

1. **bug 2 / 9 / 10 markdown leak**：用戶一眼看到 `## 近期預測反思` · 比錯數字更尷尬 · 這次發了 3 處（OperatingContextCard / Morning Note headline / SR 唸 raw）· 之前 R141 #1 修但沒包到所有出口
2. **bug 4 三 surface 打架**：用戶反覆看到「6 / 10 / 11」三個數字代表同一個概念 · 信任崩
3. **bug 5 假 EmptyState**：用戶被「等明早」哄了 · 永遠等不到 · 比直接報錯更傷信任

### 工時合計

- Critical（4 個）：3.3h
- High（5 個 · #10 含在 #2）：6.3h
- Low（4 個）：忽略 / backlog
- **Critical + High 合計：~10h**

### 建議分批

- **批次 A（2h）：bug 1 + 2 + 9 一起做** · 都是 copy / markdown leak · 改三檔
- **批次 B（3h）：bug 4 + 7 一起做** · 都圍繞 freshness / count · 改 hook + 兩個 panel
- **批次 C（3h）：bug 5 + 8 一起做** · 都圍繞 error / boundary · 改 ResearchPanel + route shell
- **批次 D（1h）：bug 3 / 6** · mobile 專修

---

## R147 結束 · Round count 9（R10 是收斂不算討論輪）

## Round 10 v2 · 真 evidence 後重寫 · 2026-04-25 17:35

> R5b/R7b/R9b 真跑後 reality-check + Claude deep dig（C 線）補 3 處。下面是給用戶的最終白話清單。

### 🔴 Critical（用戶一眼看到 · 馬上修）

| #         | 白話                                                                                                                 | 哪裡看到                 | 工時 |
| --------- | -------------------------------------------------------------------------------------------------------------------- | ------------------------ | ---- |
| **1**     | bell popup 每筆寫舊話「資料還在補齊中」(R146b 修 bell 數字 · 內文沒同步)                                             | 點右上🔔                 | 0.3h |
| **2**     | 「先盯這檔」AI 論述 raw 漏 `## ** \|`（mobile :208 + desktop :348 · 兩處）                                           | 主畫面/觀察股            | 0.5h |
| **3**     | Mobile 完全看不到「我 vs 金聯成」對比 strip（任何 viewport · today price 沒拉就消失）                                | 手機/旋轉                | 1h   |
| **4**     | 同個概念三個數字打架 11/10/11（R7b reproduce）/ 6/10/11（R1+R3）                                                     | 首頁 + 鈴鐺              | 1.5h |
| **NEW 5** | Route shell 黃 banner 漏「這頁還在搬家中」內部訊息給用戶 ([PortfolioLayout.jsx:9](src/pages/PortfolioLayout.jsx#L9)) | 任何 portfolio route URL | 0.2h |

### 🟡 High（不一眼但會撞到）

| #          | 白話                                                                                                                                                                                                                                   | 工時 |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| **6**      | 研究頁假「等明早」EmptyState 蓋掉 API 500（[ResearchPanel.jsx:1354](src/components/research/ResearchPanel.jsx#L1354) + 根因 R5b 抓的 [usePortfolioPersistence.js:294](src/hooks/usePortfolioPersistence.js#L294) 沒 abort 沒 cleanup） | 2h   |
| **7**      | iPhone landscape header 占 **49%** 螢幕（R7b 比 R3 抓 26% 更糟）+ compare strip 同時消失                                                                                                                                               | 1.5h |
| **8**      | bell 計數浮動 6/10/11（根因 R5b [usePortfolioDerivedData.js:587](src/hooks/usePortfolioDerivedData.js#L587) effect 漏 deps + 沒 abort）                                                                                                | 2h   |
| **9**      | Route page 鏈零 ErrorBoundary · 一個 Panel throw 整頁白（[PortfolioLayout.jsx](src/pages/PortfolioLayout.jsx) + [pages/\*.jsx](src/pages/) 都沒 wrap · canonical AppPanels 有）                                                        | 1.5h |
| **10**     | Morning Note headline plain render（今天空所以沒 leak · AI 一塞 markdown 就破）                                                                                                                                                        | 0.3h |
| **NEW 11** | Multi-tab watchlist sync · tab A 刪 1 檔 · tab B DOM 不更新（[R7b 抓](.tmp/r147-10-round-qa/screenshots/r7b-multi-tab-watchlist-stale-tab2.png)）                                                                                      | 1h   |
| **NEW 12** | Daily history fetch 沒 cleanup（[usePortfolioPersistence.js:258](src/hooks/usePortfolioPersistence.js#L258)）· stale data 寫回 + 錯誤吃光                                                                                              | 1h   |
| **NEW 13** | Recharts SVG `tabindex=0` 變 keyboard focus sink · tab 進去無名 trap（R9b axe-core 補抓）                                                                                                                                              | 0.8h |

`#10 SR 唸 raw markdown` 含在 `#2` 修。

### 🟢 Medium / Low

- **NEW**: Slow network 3G bootstrap shell 半舊半新（看起來載完其實還沒穩 · R7b）
- **NEW**: dead React-Query facade [src/hooks/api/useResearch.js](src/hooks/api/useResearch.js) 整層沒人 import · 0.8h 刪
- **NEW**: test coverage 空白（OperatingContextCard / ResearchPanel / DashboardPanel headline 都沒 regression test 卡這輪 bug）· 1.5h 補
- Trade modal backdrop 44% opacity（R3）
- Mobile 浮動放大鏡壓卡角（R1 R3）
- `usePortfolioPanelsContextComposer.js` 540 行巨型 hook（結構紅旗）
- 5 處吃錯誤 catch
- lint warning [dashboardHeadline.js:54](src/lib/dashboardHeadline.js#L54) `resolveFreshnessStatuses` dead helper（R146b 我改完它就死了 · 0.2h 刪）

### ⚠️ 不在這輪修（API rollout 另一 session）

8 個 API endpoint 在新 VM 都沒接好（morning-note / zscore / daily-snapshot / research / analyst-reports / trade-audit / target-prices / finmind 503/404/500 多支）· 影響 #6 / #8 trigger 頻率但不是 UI bug。

### 🤔 Friction（不是 bug · 用戶會卡）

- **F1** 三個「需/補」字概念不同但用戶感覺亂 → 修 #4
- **F2** API 失敗 EmptyState 太溫和「等明早」騙人 → 修 #6
- **F3** iPhone landscape 擠到不能用 → 修 #7
- **F4** 軟語跟 console 一堆 503/404 並存 → 修 #1#2#6

### Reality-check（R5b/R9b 真跑後撤回的）

- ❌ Claude R5「lint 爆炸」假警報 · 真跑 0 error / 1 warning（已併到 medium）
- ❌ Claude R5「viewMode contract drift」沒這事 · typecheck pass · contract clean
- ❌ Claude R9「a11y 一堆 violation」誇大 · axe-core 5 個 critical route fail = `0/0/0/0/0`
- ❌ Claude R7「沒新 evidence」是偷懶 · R7b 真跑 4 個 reproducible bug

### Top-3 最尷尬

1. **#2 + #10 markdown leak**（OperatingContextCard mobile + desktop + Morning Note headline + SR · 4 個出口 · R141 #1 修但漏全包）
2. **#4 三 surface 11/10/11 打架**（R7b 同一 screen reproduce · 用戶反覆看到「這系統怪怪的」）
3. **#5 + #6 假 EmptyState / 黃 banner**（兩 處 user-facing 都告訴用戶錯誤訊息：「這頁還在搬家中」「等明早資料」）

### 修法分批

- **A 批 ~1h**：#1 + #2 + #5 copy / markdown 三件 · 都改 src/components 文字
- **B 批 ~3h**：#4 + #8 backlog count 同源 + bell debounce + enrichment effect deps fix
- **C 批 ~3h**：#6 + #9 ResearchPanel error UI + ErrorBoundary 包到 PortfolioLayout
- **D 批 ~2h**：#3 + #7 mobile compare strip + landscape header collapse
- **E 批 ~2h**：#11 + #12 multi-tab watchlist sync + daily history abort

**Critical + High 合計 ~12h**（從 v1 估 10h → v2 增 2h 因新增 NEW 5/11/12/13）

### 工時合計

- Critical（5）：3.5h
- High（8 · #10 含在 #2）：8h
- Medium 補（slow net / dead facade / test coverage）：3.3h
- Low（5）：忽略 / backlog
- **Critical+High 合計 ~12h**（修完用戶就不會「打開網站看到問題」）

---

## R147 結束（v2）· Round 1-9 + 5b/7b/9b · 共 12 輪 + R10v2 收斂
