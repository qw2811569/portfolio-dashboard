# Runtime Contract Placeholder

R106 建立此目錄，作為 Task 2 的 schema / contract 邊界預留位置。

預期放這裡的內容：

- API request / response schemas
- shared runtime object contracts
- Zod or equivalent validation helpers

目前只放說明文件，不放 runtime code。
